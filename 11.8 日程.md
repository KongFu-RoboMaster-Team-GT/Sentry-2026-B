当前阶段的
目标：
记录日期：



---
决定：
1. MiniPC USB 分布：
正面：有网口和 DC-电源口的那一侧



2. 上下位机通信：
初步调试：USB CDC
稳定通信：USB HID 协议 
MiniPC 是 Host，STM32 是 Device

3. 下位机配置：
型号：STM32-F407-IGH6
Mode: Device Only
NVIC：中断，必须勾选
FS：全速即可，够用了
GPIO 配置：
    PA11, PA12  
    No pull-up and no pull-down
    Alternate Function Push Pull

VID = 1155
PID = 22315 → 9999

时钟配置：
Main PLL：Clock 48 MHz

1. “笔记本” 连接到 “迷你主机” 的方案：
上位机型号：：i5
密码：0000
版本：Ubuntu 22.04
显示方案：OBS（个人电脑）获取迷你主机的 HDMI 信号
设备：键盘、鼠标

USB-MicroUSB：用于连接下位机
电源线：给上位机供电
转接器 + 3-条线：
    两条.HDMI
    一条.MiniUSB-Miniusb：给转接器供电
注：
迷你主机电源侧：蓝色 USB 接口是损坏的

5. 上位机配置：
下位机 USB-CDC —— 上位机 Pyserial：使用串口进行通讯
下位机 USB-HID —— 上位机 usbipd：使用 HID 协议进行通讯

（简洁记录当前阶段的核心决定或方案、配置，不用过多解释，比如 哪个引脚用于什么硬件、开发了什么功能函数）

---
研发过程：
1. 供电方案：
MiniPC (算法侧)：需要外部电源（或电池）供电。
STM32 (电控侧)：需要电源。
大功率外设（电机、高功耗传感器）：需要高功率外部电源。

1. 调试过程；
约束 1 (性能要求)： 
    哨兵兵种需要 高频、低延迟 的实时控制（HID 的优势）。
约束 2 (初始联调要求)： 
    联调方式：USB - MiniUSB，默认使用 UART 通信（CDC 的优势）。

1. 如何协调时钟频率和通讯频率
48 MHz 时钟，正是为了驱动 12 Mbps 全速 USB (FS)

1. 上位机配置：



（这是你开发过程中的详细日志和记录，用于回溯你开发过程中）

---
信息收集：

1. 提示词：
算法组正在进行联调任务，对方是电控组成员；
算法组长：MiniPC、USB、Ubuntu 22.04；
电控组员：STM32、MiniUSB；
联调方式：USB - MiniUSB，默认使用 UART 通信；

1. 
术语：
FS：全速模式
HS：高速模式，比 FS 更快

| 接口功能 | 信号名称 | 对应 STM32 引脚 | 备注 |
| :---: | :---: | :---: | :--- |
| **USB 接口** | USB\_DM | PA11 | USB Data Minus |
| | USB\_DP | PA12 | USB Data Plus |
| | USB\_OTG | PA10 | 用于 OTG 模式识别（在 MiniPC $\leftrightarrow$ STM32 的 Device 模式下，PA10 用于配置）。 |

3. 

| 约束项 | 内容 | 影响 |
| :--- | :--- | :--- |
| **兵种** | 哨兵（轮足、SLAM、BT） | 需要高频、低延迟的里程计和控制指令通信。 |
| **算法平台** | MiniPC (Ubuntu 22.04) | USB Host 端，需要编写 Linux USB HID 驱动/库。 |
| **电控平台** | STM32 | USB Device 端，需要配置 USB HID 协议栈。 |
| **物理连接** | USB (MiniPC) $\to$ MiniUSB (STM32) | 物理链路确定，使用 USB 协议。 |
| **高度限制** | 机身总高 $\le 50$ CM | 决定了机器为矮机体，传感器布局紧凑，通信稳定性要求高。 |

4. 

| 协议 | 优点 | 缺点 | 结论 |
| :--- | :--- | :--- | :--- |
| **USB CDC (VCP)** | 实现简单，像串口一样调试。 | 需要安装驱动（在Windows上），通信延迟高，数据包无结构。 | **不适合**高频实时控制。 |
| **USB HID (Human Interface Device)** | **免驱动**（操作系统原生支持），**低延迟**（专为实时交互设计），**结构化数据**（Reports）。 | 报告描述符配置略复杂。 | **最优选择**：满足哨兵对实时性和稳定性的要求。 |

| 方向 | 术语 | 功能 | 关键需求 |
| :--- | :--- | :--- | :--- |
| **MiniPC $\to$ STM32** | **Output Report** | 发送控制指令（速度、角度、模式）。 | 必须保证指令的实时性和原子性。 |
| **STM32 $\to$ MiniPC** | **Input Report** | 发送状态反馈（里程计、IMU、电流、角度）。 | 必须保证高频、低抖动，用于 SLAM/里程计更新。 |

（收录 你学习的理论知识，不需要一定和项目相关）

---
链接：
（记录 你使用到的文件夹、网页链接，以 方便开发过程当中的再次访问）











---
反思：

1. 少了 “信息和指南” 栏
2. 
协作者工作区：（一键启动）
Keil、工作文档、说明书手册、CS、向日葵、PIX、微信传输助手
https://filehelper.weixin.qq.com/


（谱写跟整个项目无关的玩意，比如你灵机一动的想法、灵感）